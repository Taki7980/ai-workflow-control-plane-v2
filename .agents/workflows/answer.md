---
description: Read-only answer lane
---
Run `python -m ai_workflow brief "<task>"`. Use only the bounded context packet needed to answer. Do not create implementation state or modify product files.
