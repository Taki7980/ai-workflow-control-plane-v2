---
description: High-rigor full execution lane
---
Run `python -m ai_workflow brief "<task>"`. If `execution_provider: superpowers`, use the installed Superpowers workflow with the generated handoff/context packet. Otherwise use native Plan → Build → Review. CRG is context infrastructure, not the execution owner.
