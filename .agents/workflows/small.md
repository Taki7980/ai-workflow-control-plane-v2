---
description: Low-risk lightweight execution lane
---
Run `python -m ai_workflow brief "<task>"`. Use native execution, edit only known scope, run one focused verification, update the compact handoff, and capture reusable memory only when non-obvious.
