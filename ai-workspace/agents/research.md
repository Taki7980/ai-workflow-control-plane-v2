# Shared Research Cache

Store only verified discoveries that are expensive to rediscover and are not suitable for durable memory yet. Include evidence and a commit/hash when possible. Delete stale entries instead of accumulating transcripts.
